import Graphin, { Behaviors, Utils  } from '@antv/graphin';



function GraphinvisWindow({data, width, height}) {
  const { DragCanvas, ZoomCanvas, DragNode, ActivateRelations } = Behaviors;
  
}

export default GraphinvisWindow;
